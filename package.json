{
  "name": "problem-orders",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "copyfiles --flat ./src/orders.csv ./dist && copyfiles --flat ./src/articles.csv ./dist && parcel build ./src/index.html"
  },
  "dependencies": {
    "copyfiles": "^2.4.1",
    "d3": "^7.2.1",
    "parcel": "^2.0.1",
    "siema": "^1.5.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1"
  }
}
